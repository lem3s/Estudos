[project]
name = "atv02-mvc-ou-camadas"
version = "0.1.0"
description = "Simple layered architecture example (Student CRUD)"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn>=0.32.0",
]
